# Use an official Python runtime as a parent image
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /app

# Copy the requirements file into the container at /app
# requirements.txt will be generated by Makefile or should be present
COPY requirements.txt .

# Install any needed packages specified in requirements.txt
# --no-cache-dir reduces image size
# --trusted-host pypi.python.org -U pip setuptools wheel might be needed in some environments
RUN pip install --no-cache-dir -r requirements.txt

# Copy the Python source files into the container at /app
COPY math-question.py .
COPY server.py .

# Make port 8080 available to the world outside this container (Gunicorn will bind to this)
EXPOSE 8080

# Define environment variable for Gunicorn workers (optional, can be adjusted)
ENV GUNICORN_WORKERS 2

# Run the Gunicorn server
# server:app means Gunicorn will look for an 'app' object in the 'server.py' file.
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "--workers", "$GUNICORN_WORKERS", "server:app"]